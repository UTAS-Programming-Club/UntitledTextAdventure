import backend.Game;

@:nullSafety(Strict)
class TextAdventure {
  static function main() {
    // trace(getBody());
    // for (action in getActions()) {
    //   trace(action.action + ": " + action.title);
    // }

    printScreens();
  }
}
