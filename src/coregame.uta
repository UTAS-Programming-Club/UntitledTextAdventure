namespace Core;

enum ScreenDirection {
  North,
  East,
  South,
  West
}

Action TestAction {
  enum ScreenDirection direction;
}

// This is a test single line comment

/* This is a test multiline comment
Action TestAction = TestAction("Missing second param");
*/

Action QuitAction = Action("Quit Game");
Action TestAction = TestAction("Quit Game", North);
Room   FirstRoom  = Room(0, 0, "First Room!");
Screen MainMenuScreen = Screen("Untitled text adventure game", { QuitAction, TestAction });
