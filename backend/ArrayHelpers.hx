package backend;

import backend.GlobalData;
import backend.Room;

class ArrayHelpers {
  // public static function CreateRoomsArray(type: RoomType): Void {
  //   var arr = [new Room(type)];
  //   GlobalData.Init(arr);
  // }

  // public static function GetRoom(idx: Int): RoomType {
  //   return GlobalData.rooms[idx].type;
  // }
}